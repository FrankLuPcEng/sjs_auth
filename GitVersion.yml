mode: Mainline

branches:
  main:
    regex: ^main$
    increment: Patch
    tag: ''

  feature:
    regex: ^feature[/-]
    tag: useBranchName
    increment: Inherit
    source-branches: ['main']

ignore:
  sha: []
