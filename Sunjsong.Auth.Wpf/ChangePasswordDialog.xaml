<ui:FluentWindow x:Class="Sunjsong.Auth.WpfUI.ChangePasswordDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
                 xmlns:uiMarkup="clr-namespace:Wpf.Ui.Markup;assembly=Wpf.Ui"
                 mc:Ignorable="d"
                 Title="修改密碼"
                 Width="420"
                 Height="260"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False">
    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <uiMarkup:ThemesDictionary Theme="Light" />
                <uiMarkup:ControlsDictionary />
                <ResourceDictionary Source="themes/Modern.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,8">
            <StackPanel Margin="0,0,0,8">
                <TextBlock Text="舊密碼" FontWeight="SemiBold" />
                <PasswordBox PasswordChanged="OnOldPasswordChanged" />
            </StackPanel>

            <StackPanel>
                <TextBlock Text="新密碼" FontWeight="SemiBold" />
                <PasswordBox PasswordChanged="OnNewPasswordChanged" />
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,12,0,0">
            <Button Content="取消" IsCancel="True" Width="90" Margin="0,0,8,0" />
            <Button Content="套用" IsDefault="True" Width="90" Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
