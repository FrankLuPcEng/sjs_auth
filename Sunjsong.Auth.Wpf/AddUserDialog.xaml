<ui:FluentWindow x:Class="Sunjsong.Auth.WpfUI.AddUserDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
                 xmlns:uiMarkup="clr-namespace:Wpf.Ui.Markup;assembly=Wpf.Ui"
                 mc:Ignorable="d"
                 Title="新增使用者"
                 Width="520"
                 Height="520"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False">
    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <uiMarkup:ThemesDictionary Theme="Light" />
                <uiMarkup:ControlsDictionary />
                <ResourceDictionary Source="themes/Modern.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="新增使用者" FontSize="18" FontWeight="Bold" />
            <TextBlock Text="填寫帳號、名稱、描述與初始密碼；可選角色，預設啟用。" Foreground="#77000000" Margin="0,0,0,8" />
            <TextBlock Text="帳號" FontWeight="SemiBold" />
            <TextBox Text="{Binding Account, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8" />

            <TextBlock Text="名稱" FontWeight="SemiBold" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8" />

            <TextBlock Text="說明" FontWeight="SemiBold" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8" />

            <TextBlock Text="初始密碼" FontWeight="SemiBold" />
            <PasswordBox PasswordChanged="OnPasswordChanged" Margin="0,4,0,8" />
            <TextBlock Text="留空則使用預設密碼 P@ssword1!" FontSize="11" Foreground="#88000000" />

            <TextBlock Text="角色（可選）" FontWeight="SemiBold" Margin="0,8,0,0" />
            <ComboBox ItemsSource="{Binding Roles}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding SelectedRoleId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <CheckBox Content="啟用" IsChecked="{Binding IsEnabled, Mode=TwoWay}" Margin="0,8,0,0" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" IsCancel="True" Width="90" Margin="0,0,8,0" />
            <Button Content="新增" IsDefault="True" Width="90" Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
